{
  "name": "upkeepify",
  "version": "2.0.2",
  "description": "Build tooling for Upkeepify WordPress plugin",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "package:release": "bash scripts/build-release-zip.sh",
    "version:check": "node scripts/verify-version.js",
    "version:patch": "node scripts/bump-version.js patch",
    "version:minor": "node scripts/bump-version.js minor",
    "version:major": "node scripts/bump-version.js major"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
